{% extends '../formTemplate.twig' %}

{% block title %}
     Formularz Pasma
{% endblock %}

{% block header %}
    <h2>Formularz Pasma</h2>
{% endblock %}

{% block form %}
   <form id="signupForm" action="/pasma/dodaj" method="POST">
        <fieldset>
            <legend></legend>
            <div class="form-group">
                <label for="nazwaPasmo">Pasmo</label>
                <input type="text" class="form-control" id="nazwaPasmo" name="nazwaPasmo">
            </div>
            <div class="form-group">
                <label for="selectGrupa">Grupa</label>
            <select class="form-control" id="selectGrupa" name="selectGrupa" required>
                <option value="">--Wybierz Grupę--</option>
                      {% for gr in grupy %}
                  			<option value="{{gr.getIdGrupa}}" >{{gr.getNazwa}}</option>
                      {% endfor %}
				</select>
			</div>
        </fieldset>
        <br/>
            <button type="submit" class="btn btn-info"><i class="fas fa-plus">Dodaj</i></button>
    </form>

{% endblock %}